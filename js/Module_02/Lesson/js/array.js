const numbers = [1, 3, 17, 5, 9, 14, 8, 14, 34, 18, 26];
const num = 10;
let newArray;

if (numbers[] > num)

/* for (let newArray of numbers) {
  if (numbers > num); continue; {
    console.log(newArray);
  };
}; */
console.log(newArray);